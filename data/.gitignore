# Ignore all data files - these are large and machine-specific

# Input/output directories
input/
output/

# Training data (generated, very large)
training/
AIST++/
mtc/

# Any large data files
*.mp4
*.avi
*.mov
*.trc
*.csv
*.npz
*.pkl
*.pickle
*.tar.gz
*.zip

# But allow the training data archive (for sharing via Git LFS)
!training_data.tar.gz

# Keep this .gitignore
!.gitignore
